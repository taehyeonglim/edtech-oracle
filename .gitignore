# 원자료는 저작권 때문에 커밋하지 않는다. 목록(MANIFEST.md)만 공개한다.
raw/*
!raw/MANIFEST.md

node_modules/
.DS_Store
*.log

# 사이트 빌드 산출물. CI가 매번 다시 만든다. 손으로 쓴 자산은 web/에 있고 커밋한다.
dist/

# 봇 — 토큰·웹훅 URL이 들어간다
bot/config.json
bot/node_modules/
bot/state.json

# 이미지 생성 원본 — 파생본(web/assets/portraits/)만 배포한다
images/

# 봇 로그
bot/bot.log
perf-runs/run.log
